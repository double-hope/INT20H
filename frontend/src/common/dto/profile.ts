export interface AddProfileMealDTO {
    externalId: string;
}

export interface AddProfileIngredientDTO {
    externalId: string;
}

export interface DeleteProfileMealDTO {
    externalMealId: string;
}

export interface DeleteProfileIngredientDTO {
    externalIngredientId: string;
}

